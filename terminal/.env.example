# Vercel Postgres (Dashboard → Storage → Postgres → Connect)
POSTGRES_URL=

# Protect cron endpoints (Vercel sets this automatically on cron invocations)
CRON_SECRET=generate-a-long-random-string

# Optional: Slack trade alerts (same as live_momentum_scanner_slack.ipynb)
SLACK_WEBHOOK_URL=

# Optional: restrict dashboard API in production
TERMINAL_API_KEY=
